# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its crypto and simulation focus without claiming live deployment or external usage.

## Cases

- `baseline`: `fee pressure`, score 178, lane `ship`
- `stress`: `mempool churn`, score 159, lane `ship`
- `edge`: `block fit`, score 185, lane `ship`
- `recovery`: `replacement margin`, score 248, lane `ship`
- `stale`: `fee pressure`, score 235, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
